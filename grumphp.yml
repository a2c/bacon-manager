parameters:
    git_dir: .
    bin_dir: bin
    tasks: 
        phpcs: 
            standard: "deploy/rules.xml"
        git_blacklist:
            keywords:
                - "die("
                - "var_dump("
                - "exit;"
